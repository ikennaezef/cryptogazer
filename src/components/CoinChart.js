import React, {useState} from "react"

import {Box, Flex, Container, Button, Text, Heading, Link} from "@chakra-ui/react";

import {numberWithCommas} from "../utils";

const CoinChart = () => {
  return (
    <Box w={{base: 'full', md: '70%'}} pl={{base: 0, md: 3}}>
      CoinChart
    </Box>
  )
}

export default CoinChart